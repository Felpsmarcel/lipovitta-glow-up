## Remover a seção de contato

A imagem em anexo mostra a seção `ContactSection` ("Ficou com dúvida?" com WhatsApp e e-mail).

### Mudanças
1. Remover o import e o uso de `ContactSection` em `src/pages/Index.tsx`.
2. Excluir o arquivo `src/components/ContactSection.tsx`.

O botão flutuante do WhatsApp (`WhatsAppButton`) e o rodapé permanecem inalterados.